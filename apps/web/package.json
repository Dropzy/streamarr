{
  "name": "@streamarr/web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "next build",
    "dev": "next dev --hostname 0.0.0.0",
    "lint": "tsc --noEmit --project tsconfig.check.json",
    "typecheck": "tsc --noEmit --project tsconfig.check.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@streamarr/auth": "workspace:*",
    "@streamarr/config": "workspace:*",
    "@streamarr/database": "workspace:*",
    "@streamarr/events": "workspace:*",
    "@streamarr/validation": "workspace:*",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2"
  }
}
