export default function ForgotPasswordPage() {
  return (
    <main className="content">
      <h1>Forgot password</h1>
      <section className="card">
        <p className="muted">
          SMTP-based reset delivery will be enabled when email is configured.
        </p>
      </section>
    </main>
  );
}
